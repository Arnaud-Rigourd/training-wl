<div class="container">
  <h1>Writte your review</h1>

  <%= simple_form_for [@restaurant, @review] do |f| %>
    <%= f.input :raiting %>
    <%= f.input :content %>
    <%= f.submit %>
  <% end %>
</div>
